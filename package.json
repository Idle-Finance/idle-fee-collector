{
  "scripts": {
    "compile": "npx hardhat compile",
    "compile:watch": "npx nodemon --ext sol --exec 'clear && npm run compile'",
    "test": "npm run compile && npx mocha --extension ts",
    "test:nocompile": "npx mocha --extension ts",
    "test:watch": "nodemon --ext js,ts --watch test --exec 'clear &&  npm run test:nocompile'"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.1.0",
    "@openzeppelin/contracts-upgradeable": "^4.1.0",
    "@uniswap/v3-periphery": "^1.0.1",
    "hardhat": "^2.3.0"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@openzeppelin/hardhat-upgrades": "^1.8.2",
    "@typechain/ethers-v5": "^7.0.0",
    "@typechain/hardhat": "^2.0.1",
    "@types/chai": "^4.2.18",
    "@types/mocha": "^8.2.2",
    "@types/node": "^15.12.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.3.0",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "typechain": "^5.0.0",
    "typescript": "^4.3.2"
  }
}
